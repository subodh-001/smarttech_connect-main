apiVersion: v1
kind: Service
metadata:
  name: smarttech-frontend
  namespace: smarttech
  labels:
    app: smarttech-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: smarttech-frontend

